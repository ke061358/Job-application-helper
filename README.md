# Job-application-helper
somehow helpful for job application

## 使用方法

1. 解压 zip。
2. 把 `resume-outreach-tool` 文件夹放到对应的 `~/.codex/skills/` 目录。
3. 在 Codex 里说：
   ```text
   Use $resume-outreach-tool to create and run a local resume outreach web app.
4. Codex 会根据 skill 复制模板并启动网页工具。
